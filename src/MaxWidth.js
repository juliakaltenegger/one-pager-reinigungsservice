import styled from "styled-components";

const MaxWidth = styled.div`
  padding-left: 16px;
  padding-right: 16px;

  margin-left: auto;
  margin-right: auto;
  max-width: 1080px;
`;

export default MaxWidth;
